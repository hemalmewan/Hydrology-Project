paste("Enter the Required d value:",d<-as.integer(readline()))
print(d)
##validate the range of the d value it should be between 1 to 10
if(d<1 | d>10){
print("Error!!,d must be between 1 and 10")
}
##path of the nc file
url<-"C:/Hydrology-Project/Rainfall Input/NCDF/rainfall_1951_daily.nc"
##path of the csv file
url_1<-"C:/Hydrology-Project/Rainfall Input/drf_1951_new2.csv"
##save output url
save_url<-"C:/Hydrology-Project/Rainfall Input/Rx1day_1951"
r<-rast(url) ##convert raster object
dates<-as.Date(time(r)) ##time range 1951-01-01 to 1951-12-31
points<-read.csv(url_1) ##read csv file
pts<-vect(points,geom=c("lon","lat"),crs=crs(r)) ##convert the lat lon as the spatial locations
##loop through all the months January to December
##define end date and start date for each month
monthly_index<-which(format(dates,"%m")==sprintf("%02d",1))
##Daily percipitation data for each month
monthly_precipitation<-r[[monthly_index]]
##Daily precipitation data in each monthe each location
vals<-extract(monthly_precipitation,pts)
vals_mat<-as.matrix(vals[,-1]) ##remove ID column
## Compute the rolling window days
if (ncol(vals_mat) >= d) {
# Make sure vals_mat is always a matrix (even if 1 column)
vals_mat <- as.matrix(vals_mat)
# Ensure t(vals_mat) is also treated as a matrix
transposed <- t(vals_mat)
if (is.null(dim(transposed))) transposed <- matrix(transposed, nrow = 1)
roll_sum <- t(rollapply(
transposed,
width = d,
FUN = function(x) colSums(x, na.rm = TRUE),
by = 1,
partial = FALSE
))
roll_sum <- as.matrix(roll_sum)
monthly_Rxdday <- apply(roll_sum, 1, max, na.rm = TRUE)
} else {
monthly_Rxdday <- rep(NA, nrow(vals))
}
vals_mat
monthly_index
l
l
##import reuqired libraries
library(terra)
library(zoo)
##get the user input
paste("Enter the Required d value:",d<-as.integer(readline()))
print(d)
##validate the range of the d value it should be between 1 to 10
if(d<1 | d>10){
print("Error!!,d must be between 1 and 10")
}
##path of the nc file
url<-"C:/Hydrology-Project/Rainfall Input/NCDF/rainfall_1951_daily.nc"
##path of the csv file
url_1<-"C:/Hydrology-Project/Rainfall Input/drf_1951_new2.csv"
##save output url
save_url<-"C:/Hydrology-Project/Rainfall Input/Rx1day_1951"
r<-rast(url) ##convert raster object
dates<-as.Date(time(r)) ##time range 1951-01-01 to 1951-12-31
points<-read.csv(url_1) ##read csv file
pts<-vect(points,geom=c("lon","lat"),crs=crs(r)) ##convert the lat lon as the spatial locations
##define end date and start date for each month
monthly_index<-which(format(dates,"%m")==sprintf("%02d",1))
##Daily percipitation data for each month
monthly_precipitation<-r[[monthly_index]]
##Daily precipitation data in each monthe each location
vals<-extract(monthly_precipitation,pts)
vals_mat<-as.matrix(vals[,-1]) ##remove ID column
## Compute the rolling window days
if(ncol(vals_mat)>=d){
roll_sum<-t(rollapply(t(vals_mat),width=d,FUN=function(x) colSums(x,na.rm = TRUE),by=1,partial=FALSE))
roll_sum<-as.matrix(roll_sum)
monthly_Rxdday<-apply(roll_sum,1,max,na.rm=TRUE)
}else{
monthly_Rxdday<-rep(NA,nrow(vals))
}
vals_mat<-as.matrix(vals[,-1,drop=FALSE]) ##remove ID column
vals_mat
## Compute the rolling window days
if(ncol(vals_mat) >= d){
roll_sum <- apply(vals_mat, 1, function(x) rollapply(x, width = d, FUN = sum, align = "left", na.rm = TRUE))
roll_sum <- as.matrix(roll_sum)
monthly_Rxdday <- apply(roll_sum, 2, max, na.rm = TRUE)
}else{
monthly_Rxdday <- rep(NA, nrow(vals))
}
monthly_Rxdday
d
##Attach the result as points
pts$Rxdday<-monthly_Rxdday
##create the raster layers
templete<-monthly_precipitation[[1]]
Rxdday_raster<-rasterize(pts,templete,field="Rxdday")
##Save raster
file_name<-paste0(save_url,"Rx",d,"day_1951_",sprintf("%02d",month),".tif")
writeRaster(Rxdday_raster,file_name,overwrite=TRUE)
print(paste("Saved:",file_name))
##import reuqired libraries
library(terra)
library(zoo)
##get the user input
paste("Enter the Required d value:",d<-as.integer(readline()))
print(d)
##validate the range of the d value it should be between 1 to 10
if(d<1 | d>10){
print("Error!!,d must be between 1 and 10")
}
##path of the nc file
url<-"C:/Hydrology-Project/Rainfall Input/NCDF/rainfall_1951_daily.nc"
##path of the csv file
url_1<-"C:/Hydrology-Project/Rainfall Input/drf_1951_new2.csv"
##save output url
save_url<-"C:/Hydrology-Project/Rainfall Input/Rx1day_1951/"
r<-rast(url) ##convert raster object
dates<-as.Date(time(r)) ##time range 1951-01-01 to 1951-12-31
points<-read.csv(url_1) ##read csv file
pts<-vect(points,geom=c("lon","lat"),crs=crs(r)) ##convert the lat lon as the spatial locations
##define end date and start date for each month
monthly_index<-which(format(dates,"%m")==sprintf("%02d",1))
##Daily percipitation data for each month
monthly_precipitation<-r[[monthly_index]]
##Daily precipitation data in each monthe each location
vals<-extract(monthly_precipitation,pts)
vals_mat<-as.matrix(vals[,-1,drop=FALSE]) ##remove ID column
## Compute the rolling window days
if(ncol(vals_mat) >= d){
roll_sum <- apply(vals_mat, 1, function(x) rollapply(x, width = d, FUN = sum, align = "left", na.rm = TRUE))
roll_sum <- as.matrix(roll_sum)
monthly_Rxdday <- apply(roll_sum, 2, max, na.rm = TRUE)
}else{
monthly_Rxdday <- rep(NA, nrow(vals))
}
monthly_Rxdday
##Attach the result as points
pts$Rxdday<-monthly_Rxdday
##create the raster layers
templete<-monthly_precipitation[[1]]
Rxdday_raster<-rasterize(pts,templete,field="Rxdday")
##Save raster
file_name<-paste0(save_url,"Rx",d,"day_1951_",sprintf("%02d",month),".tif")
writeRaster(Rxdday_raster,file_name,overwrite=TRUE)
print(paste("Saved:",file_name))
##import reuqired libraries
library(terra)
library(zoo)
##get the user input
paste("Enter the Required d value:",d<-as.integer(readline()))
print(d)
##validate the range of the d value it should be between 1 to 10
if(d<1 | d>10){
print("Error!!,d must be between 1 and 10")
}
##path of the nc file
url<-"C:/Hydrology-Project/Rainfall Input/NCDF/rainfall_1951_daily.nc"
##path of the csv file
url_1<-"C:/Hydrology-Project/Rainfall Input/drf_1951_new2.csv"
##save output url
save_url<-"C:/Hydrology-Project/Rainfall Input/Rx1day_1951/"
r<-rast(url) ##convert raster object
dates<-as.Date(time(r)) ##time range 1951-01-01 to 1951-12-31
points<-read.csv(url_1) ##read csv file
pts<-vect(points,geom=c("lon","lat"),crs=crs(r)) ##convert the lat lon as the spatial locations
##loop through all the months January to December
for (month in 1:12) {
##define end date and start date for each month
monthly_index<-which(format(dates,"%m")==sprintf("%02d",month))
##Daily percipitation data for each month
monthly_precipitation<-r[[monthly_index]]
##Daily precipitation data in each monthe each location
vals<-extract(monthly_precipitation,pts)
vals_mat<-as.matrix(vals[,-1,drop=FALSE]) ##remove ID column
## Compute the rolling window days
if(ncol(vals_mat) >= d){
roll_sum <- apply(vals_mat, 1, function(x) rollapply(x, width = d, FUN = sum, align = "left", na.rm = TRUE))
roll_sum <- as.matrix(roll_sum)
monthly_Rxdday <- apply(roll_sum, 2, max, na.rm = TRUE)
}else{
monthly_Rxdday <- rep(NA, nrow(vals))
}
monthly_Rxdday
##Attach the result as points
pts$Rxdday<-monthly_Rxdday
##create the raster layers
templete<-monthly_precipitation[[1]]
Rxdday_raster<-rasterize(pts,templete,field="Rxdday")
##Save raster
file_name<-paste0(save_url,"Rx",d,"day_1951_",sprintf("%02d",month),".tif")
writeRaster(Rxdday_raster,file_name,overwrite=TRUE)
}
tif.files<-list.files(save_url,pattern = "\\.tif$",full.names = TRUE)
# Extract month number from file names
months <- as.numeric(gsub(".*_(\\d+)\\.tif$", "\\1", basename(tif.files)))
# Order files by month number
tif.files <- tif.files[order(months)]
##Load them as a SpatRaster files
rasters<-lapply(tif.files,rast)
rasters
##set the grid size as 4 rows and 3 columns
par(mfrow=c(4,3))
month_names=month.abb
for (month in 1:12) {
plot(rasters[[month]], main = paste("Rx1day 1952 -", month_names[month]))
}
par(mfrow=c(1,1))
##set the grid size as 4 rows and 3 columns
par(mfrow=c(4,3))
month_names=month.abb
for (month in 1:12) {
plot(rasters[[month]], main = paste("Rx1day 1951 -", month_names[month]))
}
##import reuqired libraries
library(terra)
library(zoo)
##get the user input
paste("Enter the Required d value:",d<-as.integer(readline()))
print(d)
##validate the range of the d value it should be between 1 to 10
if(d<1 | d>10){
print("Error!!,d must be between 1 and 10")
}
##path of the nc file
url<-"C:/Hydrology-Project/Rainfall Input/NCDF/rainfall_1951_daily.nc"
##path of the csv file
url_1<-"C:/Hydrology-Project/Rainfall Input/drf_1951_new2.csv"
##save output url
save_url<-"C:/Hydrology-Project/Rainfall Input/Rx1day_1951/"
r<-rast(url) ##convert raster object
dates<-as.Date(time(r)) ##time range 1951-01-01 to 1951-12-31
points<-read.csv(url_1) ##read csv file
pts<-vect(points,geom=c("lon","lat"),crs=crs(r)) ##convert the lat lon as the spatial locations
##loop through all the months January to December
for (month in 1:12) {
##define end date and start date for each month
monthly_index<-which(format(dates,"%m")==sprintf("%02d",month))
##Daily percipitation data for each month
monthly_precipitation<-r[[monthly_index]]
##Daily precipitation data in each monthe each location
vals<-extract(monthly_precipitation,pts)
vals_mat<-as.matrix(vals[,-1,drop=FALSE]) ##remove ID column
## Compute the rolling window days
if(ncol(vals_mat) >= d){
roll_sum <- apply(vals_mat, 1, function(x) rollapply(x, width = d, FUN = sum, align = "left", na.rm = TRUE))
roll_sum <- as.matrix(roll_sum)
monthly_Rxdday <- apply(roll_sum, 2, max, na.rm = TRUE)
}else{
monthly_Rxdday <- rep(NA, nrow(vals))
}
monthly_Rxdday
##Attach the result as points
pts$Rxdday<-monthly_Rxdday
##create the raster layers
templete<-monthly_precipitation[[1]]
Rxdday_raster<-rasterize(pts,templete,field="Rxdday")
##Save raster
file_name<-paste0(save_url,"Rx",d,"day_1951_",sprintf("%02d",month),".tif")
writeRaster(Rxdday_raster,file_name,overwrite=TRUE)
}
par(mfrow=c(1,1))
##set the grid size as 4 rows and 3 columns
par(mfrow=c(4,3))
month_names=month.abb
for (month in 1:12) {
plot(rasters[[month]], main = paste("Rx3day 1951 -", month_names[month]))
}
monthly_Rxdday
##import reuqired libraries
library(terra)
library(zoo)
##path of the nc file
url<-"C:/Hydrology-Project/Rainfall Input/NCDF/rainfall_1951_daily.nc"
##path of the csv file
url_1<-"C:/Hydrology-Project/Rainfall Input/drf_1951_new2.csv"
##path of the nc file
url<-"C:/Hydrology-Project/Rainfall Input/NCDF/rainfall_1951_daily.nc"
##path of the csv file
url_1<-"C:/Hydrology-Project/Rainfall Input/drf_1951_new2.csv"
##save output url
save_url<-"C:/Hydrology-Project/Rainfall Input/PRCPTOT_1951"
r<-rast(url) ##convert raster object
dates<-as.Date(time(r)) ##time range 1951-01-01 to 1951-12-31
points<-read.csv(url_1) ##read csv file
pts<-vect(points,geom=c("lon","lat"),crs=crs(r)) ##convert the lat lon as the spatial locations
##define end date and start date for each month
monthly_index<-which(format(dates,"%m")==sprintf("%02d",1))
##Daily precipitation data for each month
monthly_precipitation<-r[[monthly_index]]
##Daily precipitation data in each month each location
vals<-extract(monthly_precipitation,pts)
vals
vals<-vals[,-1,drop=FALSE] ##remove ID column
vals
class(vals)
vals
vals[1,1]
vals[1,]
vals[357,]
vals[358,]
##import reuqired libraries
library(terra)
library(zoo)
##Total Precipitation per Month Jan-DEC Locations 357
##path of the nc file
url<-"C:/Hydrology-Project/Rainfall Input/NCDF/rainfall_1951_daily.nc"
##path of the csv file
url_1<-"C:/Hydrology-Project/Rainfall Input/drf_1951_new2.csv"
##save output url
save_url<-"C:/Hydrology-Project/Rainfall Input/PRCPTOT_1951"
r<-rast(url) ##convert raster object
dates<-as.Date(time(r)) ##time range 1951-01-01 to 1951-12-31
points<-read.csv(url_1) ##read csv file
pts<-vect(points,geom=c("lon","lat"),crs=crs(r)) ##convert the lat lon as the spatial locations
##define end date and start date for each month
monthly_index<-which(format(dates,"%m")==sprintf("%02d",1))
##Daily precipitation data for each month
monthly_precipitation<-r[[monthly_index]]
##Daily precipitation data in each month each location
vals<-extract(monthly_precipitation,pts)
vals<-vals[,-1,drop=FALSE] ##remove ID column
vals[351,]
vals[357,]
vals[344,]
vals[67,]
a<-c(2,4,6,5,6)
A>3
a>3
a[a>3]
sum(a[a>3])
##define threshold value
threshold<-1
##import reuqired libraries
library(terra)
library(zoo)
##Total Precipitation per Month Jan-DEC Locations 357
##define threshold value
threshold<-1
##path of the nc file
url<-"C:/Hydrology-Project/Rainfall Input/NCDF/rainfall_1951_daily.nc"
##path of the csv file
url_1<-"C:/Hydrology-Project/Rainfall Input/drf_1951_new2.csv"
##save output url
save_url<-"C:/Hydrology-Project/Rainfall Input/PRCPTOT_1951"
r<-rast(url) ##convert raster object
dates<-as.Date(time(r)) ##time range 1951-01-01 to 1951-12-31
points<-read.csv(url_1) ##read csv file
pts<-vect(points,geom=c("lon","lat"),crs=crs(r)) ##convert the lat lon as the spatial locations
##define end date and start date for each month
monthly_index<-which(format(dates,"%m")==sprintf("%02d",1))
##Daily precipitation data for each month
monthly_precipitation<-r[[monthly_index]]
##Daily precipitation data in each month each location
vals<-extract(monthly_precipitation,pts)
vals<-vals[,-1,drop=FALSE] ##remove ID column
for (row in 1:357) {
location<-vals[row,]
PRCPTOT[row]<-sum(location[location>threshold])
}
##import reuqired libraries
library(terra)
library(zoo)
##Total Precipitation per Month Jan-DEC Locations 357
##define threshold value
threshold<-1
##path of the nc file
url<-"C:/Hydrology-Project/Rainfall Input/NCDF/rainfall_1951_daily.nc"
##path of the csv file
url_1<-"C:/Hydrology-Project/Rainfall Input/drf_1951_new2.csv"
##save output url
save_url<-"C:/Hydrology-Project/Rainfall Input/PRCPTOT_1951"
r<-rast(url) ##convert raster object
dates<-as.Date(time(r)) ##time range 1951-01-01 to 1951-12-31
points<-read.csv(url_1) ##read csv file
pts<-vect(points,geom=c("lon","lat"),crs=crs(r)) ##convert the lat lon as the spatial locations
##define end date and start date for each month
monthly_index<-which(format(dates,"%m")==sprintf("%02d",1))
##Daily precipitation data for each month
monthly_precipitation<-r[[monthly_index]]
##Daily precipitation data in each month each location
vals<-extract(monthly_precipitation,pts)
vals<-vals[,-1,drop=FALSE] ##remove ID column
PRCPTOT<-c()
for (row in 1:357) {
location<-vals[row,]
PRCPTOT[row]<-sum(location[location>threshold])
}
PRCPTOT
##import reuqired libraries
library(terra)
library(zoo)
##Total Precipitation per Month Jan-DEC Locations 357
##define threshold value
threshold<-1
##path of the nc file
url<-"C:/Hydrology-Project/Rainfall Input/NCDF/rainfall_1951_daily.nc"
##path of the csv file
url_1<-"C:/Hydrology-Project/Rainfall Input/drf_1951_new2.csv"
##save output url
save_url<-"C:/Hydrology-Project/Rainfall Input/PRCPTOT_1951"
r<-rast(url) ##convert raster object
dates<-as.Date(time(r)) ##time range 1951-01-01 to 1951-12-31
points<-read.csv(url_1) ##read csv file
pts<-vect(points,geom=c("lon","lat"),crs=crs(r)) ##convert the lat lon as the spatial locations
library(terra)
library(zoo)
## threshold for PRCPTOT (rain > threshold mm count)
threshold <- 1
## input files
url <- "C:/Hydrology-Project/Rainfall Input/NCDF/rainfall_1951_daily.nc"
url_1 <- "C:/Hydrology-Project/Rainfall Input/drf_1951_new2.csv"
## output folder (no trailing slash)
save_url <- "C:/Hydrology-Project/Rainfall Input/PRCPTOT_1951/"
## Read raster and time vector
r <- rast(url)
dates <- as.Date(time(r))
## Read point stations
points <- read.csv(url_1)
pts <- vect(points, geom = c("lon", "lat"), crs = crs(r))
## Loop for each month (1 to 12)
for(month in 1:12){
## Identify daily bands for this month
monthly_index <- which(format(dates, "%m") == sprintf("%02d", month))
monthly_precipitation <- r[[monthly_index]]
## Extract precipitation at stations
vals <- extract(monthly_precipitation, pts)
vals <- vals[,-1, drop = FALSE]  # remove ID column, keep matrix
## Compute PRCPTOT per station
PRCPTOT <- apply(vals, 1, function(x) sum(x[x > threshold], na.rm = TRUE))
## Attach to points
pts$PRCPTOT <- PRCPTOT
## Rasterize output
template <- monthly_precipitation[[1]]
PRCPTOT_raster <- rasterize(pts, template, field = "PRCPTOT")
## Save raster
file_name <- paste0(save_url, "_PRCPTOT_1951_", sprintf("%02d", month), ".tif")
writeRaster(PRCPTOT_raster, file_name, overwrite = TRUE)
cat("Saved:", file_name, "\n")
}
##import reuqired libraries
library(terra)
library(zoo)
##Total Precipitation per Month Jan-DEC Locations 357
##define threshold value
threshold<-1
##path of the nc file
url<-"C:/Hydrology-Project/Rainfall Input/NCDF/rainfall_1951_daily.nc"
##path of the csv file
url_1<-"C:/Hydrology-Project/Rainfall Input/drf_1951_new2.csv"
##save output url
save_url<-"C:/Hydrology-Project/Rainfall Input/PRCPTOT_1951"
r<-rast(url) ##convert raster object
dates<-as.Date(time(r)) ##time range 1951-01-01 to 1951-12-31
points<-read.csv(url_1) ##read csv file
pts<-vect(points,geom=c("lon","lat"),crs=crs(r)) ##convert the lat lon as the spatial locations
library(terra)
library(zoo)
## threshold for PRCPTOT (rain > threshold mm count)
threshold <- 1
## input files
url <- "C:/Hydrology-Project/Rainfall Input/NCDF/rainfall_1951_daily.nc"
url_1 <- "C:/Hydrology-Project/Rainfall Input/drf_1951_new2.csv"
## output folder (no trailing slash)
save_url <- "C:/Hydrology-Project/Rainfall Input/PRCPTOT_1951/"
## Read raster and time vector
r <- rast(url)
dates <- as.Date(time(r))
## Read point stations
points <- read.csv(url_1)
pts <- vect(points, geom = c("lon", "lat"), crs = crs(r))
## Loop for each month (1 to 12)
for(month in 1:12){
## Identify daily bands for this month
monthly_index <- which(format(dates, "%m") == sprintf("%02d", month))
monthly_precipitation <- r[[monthly_index]]
## Extract precipitation at stations
vals <- extract(monthly_precipitation, pts)
vals <- vals[,-1, drop = FALSE]  # remove ID column, keep matrix
## Compute PRCPTOT per station
PRCPTOT <- apply(vals, 1, function(x) sum(x[x >= threshold], na.rm = TRUE))
## Attach to points
pts$PRCPTOT <- PRCPTOT
## Rasterize output
template <- monthly_precipitation[[1]]
PRCPTOT_raster <- rasterize(pts, template, field = "PRCPTOT")
## Save raster
file_name <- paste0(save_url, "_PRCPTOT_1951_", sprintf("%02d", month), ".tif")
writeRaster(PRCPTOT_raster, file_name, overwrite = TRUE)
cat("Saved:", file_name, "\n")
}
tif.files<-list.files(save_url,pattern = "\\.tif$",full.names = TRUE)
# Extract month number from file names
months <- as.numeric(gsub(".*_(\\d+)\\.tif$", "\\1", basename(tif.files)))
# Order files by month number
tif.files <- tif.files[order(months)]
##Load them as a SpatRaster files
rasters<-lapply(tif.files,rast)
rasters
par(mfrow=c(1,1))
##set the grid size as 4 rows and 3 columns
par(mfrow=c(4,3))
month_names=month.abb
for (month in 1:12) {
plot(rasters[[month]], main = paste("Rx3day 1951 -", month_names[month]))
}
month_names=month.abb
for (month in 1:12) {
plot(rasters[[month]], main = paste("PRCPTOT 1951 -", month_names[month]))
}
